<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<definition>
    <body><![CDATA[<?php

class %BASECLASSNAME% extends dbPeer
{
    protected	$instance		= false;
    
    protected	$tableName		=	'%TABLENAME%';
    protected	$primaryKey		=	%PRIMARYKEY%;
    protected	$primaryTKey	=	%PRIMARYTKEY%;
    protected	$primaryOKey	=	%PRIMARYOKEY%;
	protected	$fields			=	array(%FIELDS%);
    protected	$primaryBind	=	"%PRIMARYBIND%";
    protected	$multiPrimary	=	%MULTIPRIMARY%;

    public static function setKey( $key )
    {
        return dbPeer::getInstance('%CLASSNAME%')->doSetKey( $key );
    }

    /**
     * get full row from table
     *
     * @param integer $primaryKey
     * @return array
     */
    public static function  row( $primaryKey )
    {
        return dbPeer::getInstance('%CLASSNAME%')->doRow( $primaryKey );
    }

    /**
     * get full rows from table
     *
     * @param integer $primaryKey
     * @return array
     */
    public static function  rows( $primaryKeys )
    {
        return dbPeer::getInstance('%CLASSNAME%')->doRows( $primaryKeys );
    }

    /**
     * get array of primary keys
     *
     * @param array $where
     * @param array $bind
     * @param array $order
     * @param array $limit
     * @return array
     */
    public static function  cols( $where = array(), $order = array(), $limit = false, $offset = false, &$total = false, &$more = false )
    {
        return dbPeer::getInstance('%CLASSNAME%')->doCols( $where, $order, $limit, $offset, $total, $more);
    }


    /**
     * insert row
     *
     * @param array $data
     * @return integer
     */
    public static function  insert( $data, $multi = false )
    {
        return dbPeer::getInstance('%CLASSNAME%')->doInsert( $data, $multi );
    }


    /**
     * Replace row
     *
     * @param array $data
     * @return integer
     */
    public static function  replace( $data )
    {
        return dbPeer::getInstance('%CLASSNAME%')->doReplace( $data );
    }


    /**
     * update row by primary key
     *
     * @param integer $id
     * @param array $data
     * @return boolean
     */
    public static function  update($id,  $data)
    {
        return dbPeer::getInstance('%CLASSNAME%')->doUpdate($id,  $data);
    }

    /**
     * delete row
     *
     * @param integer $primaryKey
     */
    public static function  delete( $primaryKey )
    {
        return dbPeer::getInstance('%CLASSNAME%')->doDelete( $primaryKey );
    }

	static function setMeta($id, $values)
	{
		$item	=	dbPeer::getInstance('%CLASSNAME%')->doRow($id);
		$meta	=	$item['meta'];

		foreach ($values as $key => $value)
		{
			$meta[$key]	=	$value;
		}

		dbPeer::getInstance('%CLASSNAME%')->doUpdate($id, array('meta' => json_encode($meta)));
	}

	static function getMeta($id, $key, $item = false)
	{
		if (!$item)
		{
			$item	=	dbPeer::getInstance('%CLASSNAME%')->doRow($id);
		}

		if ($item['meta'])
		{
			return $item['meta'][$key];
		}

		return false;
	}


}

]]>
    </body>
</definition>
